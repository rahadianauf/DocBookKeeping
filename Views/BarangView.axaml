<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:DocBookKeeping.ViewModels"
             x:Class="DocBookKeeping.Views.BarangView"
             x:DataType="vm:BarangViewModel">
  <ScrollViewer>
    <StackPanel Spacing="24" Margin="0,0,0,24">

      <Grid ColumnDefinitions="*,Auto">
        <StackPanel Spacing="6">
          <TextBlock Text="Barang" FontSize="30" FontWeight="Bold" Foreground="#111827" />
          <TextBlock Text="Kelola data master barang" FontSize="14" Foreground="#6B7280" />
        </StackPanel>
        <Button Grid.Column="1" Content="⟳  Refresh" Command="{Binding LoadBarangCommand}"
                Padding="18,10" Background="#4F46E5" Foreground="White"
                BorderThickness="0" CornerRadius="8" VerticalAlignment="Center" />
      </Grid>

      <Border Background="White" CornerRadius="14" Padding="20" BoxShadow="0 1 3 0 #0A000000" Width="260" HorizontalAlignment="Left">
        <Grid ColumnDefinitions="Auto,*">
          <Border Width="42" Height="42" CornerRadius="10" Background="#EEF2FF">
            <TextBlock Text="◆" FontSize="16" Foreground="#4F46E5" HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
          <StackPanel Grid.Column="1" Margin="14,0,0,0" VerticalAlignment="Center">
            <TextBlock Text="Total Barang" FontSize="12.5" Foreground="#6B7280" />
            <TextBlock Text="{Binding JumlahBarang}" FontSize="21" FontWeight="Bold" Foreground="#111827" />
          </StackPanel>
        </Grid>
      </Border>

      <Border Background="White" CornerRadius="14" Padding="22" BoxShadow="0 1 3 0 #0A000000">
        <StackPanel Spacing="14">
          <TextBlock Text="{Binding FormModeLabel}" FontWeight="SemiBold" FontSize="15" Foreground="#111827" />
          <Rectangle Height="1" Fill="#F3F4F6" />

          <StackPanel Orientation="Horizontal" Spacing="10">
            <StackPanel Spacing="4">
              <TextBlock Text="Nama Barang" FontSize="11" Foreground="#6B7280" />
              <TextBox Text="{Binding FormNamaBarang}" Width="220" />
            </StackPanel>
            <StackPanel Spacing="4">
              <TextBlock Text="Kategori" FontSize="11" Foreground="#6B7280" />
              <ComboBox ItemsSource="{Binding KategoriOptions}" SelectedItem="{Binding FormKategori}" Width="180">
                <ComboBox.ItemTemplate>
                  <DataTemplate><TextBlock Text="{Binding Kategori}" /></DataTemplate>
                </ComboBox.ItemTemplate>
              </ComboBox>
            </StackPanel>
            <StackPanel Spacing="4">
              <TextBlock Text="Satuan" FontSize="11" Foreground="#6B7280" />
              <ComboBox ItemsSource="{Binding SatuanOptions}" SelectedItem="{Binding FormSatuan}" Width="150">
                <ComboBox.ItemTemplate>
                  <DataTemplate><TextBlock Text="{Binding Satuan}" /></DataTemplate>
                </ComboBox.ItemTemplate>
              </ComboBox>
            </StackPanel>
          </StackPanel>

          <StackPanel Orientation="Horizontal" Spacing="10">
            <Button Content="Tambah" Command="{Binding AddBarangCommand}"
                    Background="#16A34A" Foreground="White" BorderThickness="0" Padding="18,10" CornerRadius="8" />
            <Button Content="Simpan Perubahan" Command="{Binding UpdateBarangCommand}"
                    Background="#4F46E5" Foreground="White" BorderThickness="0" Padding="18,10" CornerRadius="8" />
            <Button Content="Hapus" Command="{Binding DeleteBarangCommand}"
                    Background="#FEE2E2" Foreground="#DC2626" BorderThickness="0" Padding="18,10" CornerRadius="8" />
            <Button Content="Batal" Command="{Binding ClearFormCommand}"
                    Background="Transparent" Foreground="#6B7280" BorderThickness="1" BorderBrush="#E5E7EB"
                    Padding="18,10" CornerRadius="8" />
          </StackPanel>
        </StackPanel>
      </Border>

      <TextBlock Text="{Binding ErrorMessage}" Foreground="#DC2626" TextWrapping="Wrap"
                 IsVisible="{Binding ErrorMessage, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" />

      <Border Background="White" CornerRadius="14" Padding="22" BoxShadow="0 1 3 0 #0A000000">
        <StackPanel Spacing="14">
          <Grid ColumnDefinitions="Auto,*,Auto">
            <TextBlock Text="◆" FontSize="14" Foreground="#4F46E5" VerticalAlignment="Center" />
            <TextBlock Grid.Column="1" Text="Daftar Barang" FontWeight="SemiBold" Foreground="#111827" Margin="8,0,0,0" VerticalAlignment="Center" />
            <TextBox Grid.Column="2" Text="{Binding SearchText}" PlaceholderText="Cari nama barang..." Width="220" />
          </Grid>
          <Rectangle Height="1" Fill="#F3F4F6" />
          <DataGrid ItemsSource="{Binding BarangList}" SelectedItem="{Binding SelectedBarang}"
                    AutoGenerateColumns="False" GridLinesVisibility="Horizontal" IsReadOnly="True" MinHeight="350">
            <DataGrid.Styles>
              <Style Selector="DataGridCell TextBlock"><Setter Property="Foreground" Value="#111827" /></Style>
              <Style Selector="DataGridColumnHeader">
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Foreground" Value="#6B7280" />
              </Style>
            </DataGrid.Styles>
            <DataGrid.Columns>
              <DataGridTextColumn Header="No" Binding="{Binding No}" Width="55" />
              <DataGridTextColumn Header="Nama Barang" Binding="{Binding NamaBarang}" Width="*" />
              <DataGridTextColumn Header="Kategori" Binding="{Binding IdKategoriNavigation.Kategori}" Width="180" />
              <DataGridTextColumn Header="Satuan" Binding="{Binding IdSatuanNavigation.Satuan}" Width="120" />
            </DataGrid.Columns>
          </DataGrid>
        </StackPanel>
      </Border>

    </StackPanel>
  </ScrollViewer>
</UserControl>